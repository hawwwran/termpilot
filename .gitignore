# Locally-generated, machine-specific, or sensitive files.
relay/config.php
relay/data/
relay/logs/
server-logs/
__pycache__/
*.pyc
# Crypto vectors are regenerated by linux/tests/test_crypto.py --gen-vectors.
# They contain a known-test password (no secret leaked) but checking
# them in would be noisy.
linux/tests/crypto_vectors.json
# Build artefacts from tools/build-release.sh.
dist/
# Playwright MCP scratch artifacts from local browser-driven testing.
.playwright-mcp/
